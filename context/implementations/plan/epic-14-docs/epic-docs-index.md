# Epic 14 — Final documentation

Always the very last epic. During implementation only `context/` (and the progress journal) is maintained; once everything is mature, produce the engineer-first detail documentation in `documentation/` per `context/00-documentation-instructions.md`.

## Story 14.1 — Documentation pass

- Task 14.1.1 final documentation: bring `documentation/services/` in line with the final codebase, refresh `context/` overviews, update `context/00-index.md`, archive obsolete docs.

## Exit criteria

A newcomer can onboard from `context/00-project-complete-overview.md` and drill to accurate detail files; no doc references dead code.
