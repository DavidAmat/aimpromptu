# Task 14.1.1 — Final documentation

## Subtask 14.1.1.1 — Detail tree

Rewrite/extend `documentation/services/backend/` and `documentation/services/frontend/` to match the final code: endpoints, schemas, matrix engine modules, transcription pipeline, storage layout, score format, UI components. Follow the two-folder model and cross-linking rules of `context/00-documentation-instructions.md`.

## Subtask 14.1.1.2 — Context refresh

Update `context/00-project-complete-overview.md`, `01-project.md`, `03-services-overview.md`, `07-database.md` (storage is no longer a single JSON file) and `00-index.md`. Move superseded docs to `archive/` or `deprecated/` with banners as required.

## Subtask 14.1.1.3 — Progress journal closure

Summarize the `context/implementations/progress/` journal into a short retrospective; the checklist becomes the historical record of what shipped and what was cancelled.

## Acceptance

Doc index complete; spot-check that every documented command runs.
