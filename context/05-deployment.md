# Deployment

POC is local-only. No deploy pipeline or hosted environment exists yet.

**Planned (not built):** run `aitu-backend` and `aitu-frontend` as separate Docker containers from this
monorepo, with proper service boundaries and orchestration added when we move past POC.
