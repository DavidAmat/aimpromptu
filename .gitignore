# macOS
.DS_Store

# Service subrepos (each has its own git history)
aitu-backend/
aitu-frontend/

# Legacy folder names (Vite cache may recreate at old path)
piano-matrix-generation/
piano-matrix-notation/

# Local agent config (optional; not part of docs tree)
.claude/
